Specification modes {
	Clock Mode1, Mode2, Trigger, ReactionTime, Delay [
		
		Let Mode be Mode1 + Mode2
		Exclusion Mode1 # Mode2
		
		Delay = Trigger $ 1
		
		Precedence Mode < Trigger <= Delay
	]
}